---
kind: pipeline
name: default

steps:
  - name: gate-master
    image: bdebyl/alpine-git
    commands:
      - git fetch origin +refs/heads/master
      - git show-ref
      - git diff --name-only HEAD~1..HEAD | grep -E 'Dockerfile|thumbr.sh' || exit 78
    when:
      branch:
        - master

  - name: gate-pr
    image: bdebyl/alpine-git
    commands:
      - git fetch origin +refs/heads/master
      - git show-ref
      - git diff --name-only $(git merge-base refs/remotes/origin/master HEAD)..HEAD | grep -E 'Dockerfile|thumbr.sh' || exit 78
    when:
      branch:
        exclude:
          - master

  - name: publish
    image: plugins/docker
    settings:
      username:
        from_secret: docker_username
      password:
        from_secret: docker_password
      repo: bdebyl/thumbr
    when:
      branch:
        - master
---
kind: signature
hmac: b5b3d5188c1e889d08f6ff760620ea43202fa7266f7b53eaa6331d72afd5aed3

...
